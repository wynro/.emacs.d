;;; discover-config.el --- discover.el configuration
;;; Commentary:
;;;
;;; Code:

(use-package discover
  :ensure t
  :init
  (global-discover-mode 1)
)

(provide 'discover-config)
;;; discover-config.el ends here
