;;; projectile-mode-config.el --- projectile-mode configuration
;;; Commentary:
;;;
;;; Code:

(use-package projectile
  :ensure t
  :bind (("C-c p" . projectile-mode))
  )

(provide 'projectile-mode-config)
;;; projectile-mode-config.el ends here
