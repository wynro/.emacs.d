;;; minimap-config.el --- Configuration for minimap-mode
;;; Commentary:
;;; long description
;;; Code:

(require 'minimap)
(global-set-key (kbd "C-#") 'minimap-mode)

(provide 'minimap-config)
;;; minimap-config.el ends here
