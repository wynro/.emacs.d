# -*- mode: snippet -*-
# name: usage
# key: usage
# --
# Show command instructions and exit with the given code
usage () {
    cat <<EOF
\${0} [-h] $0

  ${1:description}

Options:
  -h
        Show this usage
EOF
}